import Label from "../ui/Label"

// Task List Header component that renders on the top of the task list.
const ListHeader = () => (
  <Label className="font-medium text-sm text-gray-400">
    Press and hold a task to see more options
  </Label>
)

export default ListHeader
